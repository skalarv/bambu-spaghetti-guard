# Single source of truth for dependencies is pyproject.toml.
# This file exists so `pip install -r requirements.txt` keeps working:
# it installs the package editable with test + lint/type tooling.
#
# Live inference (GPU) extras are separate — see docs/INSTALL.md:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install -e .[live]
-e .[dev]
